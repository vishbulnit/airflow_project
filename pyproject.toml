[project]
name = "airflow-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Vishnukumar Vishwakarma", email = "vishwakarma91@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "apache-airflow>=3.3.0",
]

[project.scripts]
airflow-project = "airflow_project:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
